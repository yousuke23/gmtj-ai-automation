# 初日チェックリスト（チェックを付けながら進める）

印刷しなくてよいです。Cursor でこのファイルを開いて、完了したら `[ ]` を `[x]` に変えて保存してください。

## Mac

- [ ] Finder で `デスクトップ` → `GMTJ-AI-Automation` を開ける
- [ ] その中に `CLAUDE.md` があるのを目で確認した

## 開発者ツール（git 用）

- [ ] 「コマンドラインデベロッパツール」のインストールが終わった
- [ ] ターミナルで `git --version` が通った

## Cursor

- [ ] Cursor をインストールして起動できた
- [ ] `File` → `Open Folder` で `GMTJ-AI-Automation` を開いた
- [ ] 左にファイル一覧が見える

## ターミナル（Cursor 内で OK）

- [ ] `cd /Users/tarnar/Desktop/GMTJ-AI-Automation` を実行した
- [ ] `pwd` の結果が `/Users/tarnar/Desktop/GMTJ-AI-Automation` だった

## Git（任意だが強く推奨）

- [ ] `git config --global user.name` を設定した
- [ ] `git config --global user.email` を設定した
- [ ] `git init` を実行した
- [ ] `git add .` → `git commit` までできた

## Claude Code

- [ ] `claude --version` が通った
- [ ] `claude` を起動してログインまで終わった
- [ ] `START_HERE-超初心者.md` の「7-3」の例文を一度送った

---

全部 `[x]` になったら「初日クリア」です。お疲れさまでした。

---

## 任意：2日目（中身を触る）

- [ ] `kb/policy.md` をざっと読んだ（長くても最初の「エスカレーション」まででOK）
- [ ] `kb/templates/README.md` を開き、ファイル一覧を眺めた
- [ ] `eval/golden-questions.md` を開き、質問の雰囲気を眺めた
- [ ] `deploy/openwebui-knowledge.md` を開いた（読むだけでOK。Docker無しでも可）
- [ ] （Docker あり）`bash deploy/package-kb-for-knowledge.sh` を実行して ZIP ができた
- [ ] （Docker あり・さらに進む）`deploy/caddy/README.md` をざっと読んだ（HTTPS 終端のイメージ用）
- [ ] `make ci`（`verify-templates` + `verify-golden`）が **成功**する
- [ ] （任意）**[ゴールデン質問の採点ルーブリック](../eval/scoring-rubric.md)** を眺めた
